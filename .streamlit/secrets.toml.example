# Streamlit Cloud → App settings → Secrets (TOML)

# --- Recommended: Railway backend + Streamlit frontend ---
BACKEND_MODE = "http"
BACKEND_URL = "https://your-service.up.railway.app"

# --- All-in-one (no Railway) ---
# BACKEND_MODE = "local"
# GROQ_API_KEY = "gsk_..."
# INGEST_LIMIT = "500"
